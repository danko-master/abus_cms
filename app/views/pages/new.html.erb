<h1><%= t('views.page.page_create_welcome') %></h1>

<%= render 'layouts/domain_menu' %>

<%= form_for(@page) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="div-actions">
    <%= f.submit t('views.buttons.create') %>
  </div>
<% end %>